# Server-specific configuration overrides
# This file can be loaded after default.env for production deployments

# Production Performance Settings
WHISPER_MODEL=ggml-tiny.bin
MAX_UPLOAD_SIZE=500M
TRANSCRIPTION_TIMEOUT=7200

# Server Resource Limits
API_MEMORY_LIMIT=512M
WHISPER_MEMORY_LIMIT=1200M

# Production URLs (update these for your domain)
# EXTERNAL_URL=https://transcribe.yourdomain.com
# API_URL=https://transcribe.yourdomain.com/api

# Production Security (if needed)
# ALLOWED_ORIGINS=https://transcribe.yourdomain.com
# RATE_LIMIT_REQUESTS=10
# RATE_LIMIT_WINDOW=60

# Monitoring (optional integrations)
# METRICS_ENABLED=true
# LOG_LEVEL=info

# Backup Settings (if implementing automated backups)
# BACKUP_ENABLED=true
# BACKUP_RETENTION_DAYS=30
# BACKUP_S3_BUCKET=your-backup-bucket